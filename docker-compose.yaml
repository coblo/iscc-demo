version: '3'

services:
  iscc-service:
    image: titusz/iscc-service:0.1.4
    restart: unless-stopped
    ports:
      - 8000:8080
  iscc-demo:
    image: iscc-service
    build:
      context: .
      dockerfile: .ci/Dockerfile
    restart: unless-stopped
    depends_on:
      - iscc-service
    ports:
      - 8080:80
